<section style="padding: 120px 0; text-align: center;">
  <div class="container" style="max-width: 560px;">
    <span class="eyebrow">{{ 'general.404.title' | t }}</span>
    <h1 style="margin: 16px 0 16px;">This page wandered off.</h1>
    <p style="color: var(--ink-soft); margin-bottom: 32px;">{{ 'general.404.subtext' | t }}</p>
    <a href="{{ routes.root_url }}" class="btn btn-primary">{{ 'general.404.link' | t }}</a>
  </div>
</section>
